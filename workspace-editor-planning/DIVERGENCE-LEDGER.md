# Fork divergence ledger

**Why this exists.** `feature/workspace-editor` edits stock Godot editor files.
When we sync onto a newer upstream, the rebase conflicts land in exactly these
files — and `node_3d_editor_plugin.cpp` / `editor_node.cpp` are among *upstream's*
highest-churn files too. Knowing, per file, which hunks are **ours by design**
(structural, keep and re-apply) vs **incidental** (small, upstreamable, or soon
to be deleted) is what turns a rebase from days into hours.

Baseline: branch `feature/workspace-editor`, forked from `master` @ `895db87388`.
Regenerate the raw list with `git diff --name-status master...HEAD`.

**Rebase cadence policy.** Rebase onto upstream **at milestone boundaries only**
(e.g. after G2 lands), **never mid-surgery**. A mid-extraction rebase of
`node_3d_editor_plugin.cpp` — with half the services/view split applied — would
be miserable. Finish the current step to a green commit first, then rebase.

Legend — **Class**: `STRUCTURAL` = ours by design, will conflict, re-apply
deliberately · `SHIM` = intermediate scaffolding, will be rewritten at a later
step (don't over-invest in clean rebasing) · `UPSTREAMABLE` = a stock bug we
fixed; PR it and the hunk leaves our diff · `NEW` = file we added, no conflict
surface. **Risk** = expected rebase pain.

---

## Modified stock files (the conflict surface)

| File | Class | Risk | What diverged / rebase note |
|------|-------|------|------------------------------|
| `editor/scene/3d/node_3d_editor_plugin.{cpp,h}` | STRUCTURAL | **HIGHEST** | The services/view split (Step ③/①). New `Node3DEditorView` class owns viewports + `bound_world` + grid/origin decoration + resource lifecycle; `Node3DEditor` retains services and forwards accessors. Churn concentrates in: the ctor (`_build_view_viewports`, `main_view`), `_init_indicators`/`_finish_indicators`, `set_active_world`, `get_editor_world_3d`/`_scenario`/`_space_state`, and ~25 rerouted viewport accessors. Tagged `// Step③`/`// Step①`. On rebase: expect conflicts anywhere upstream touches viewport plumbing or indicator init; re-apply the *view ownership* intent, not line-by-line. |
| `editor/editor_node.{cpp,h}` | STRUCTURAL | **HIGH** | The multi-document spine. Added `documents_holder` (persistent in-tree parent keeping every open doc's `scene_root` live — including the placeholder), `register_document_context`, `_activate_scene_views`, and `get_scene_root()` delegating to the active document. `editor_node.cpp` is one of upstream's most-churned files. ⑤c RETIRED the `_display_scene_root` reparent shim: scene_roots stay parked under `documents_holder` for their whole lifetime; `_activate_scene_views` now binds the 2D main view to the active document via `CanvasItemEditor::activate_document()` (own world-bound viewport, no reparenting), mirroring the 3D `set_active_world`. `_activate_scene_views` is still SCAFFOLDING (retire when DocumentView owns activation) but the doc-context registration is STRUCTURAL. **G4:** constructs `workspace_file_drawer` as a top-level child of `center_vb` (bracketed insert near the center_split build) and wires its `FileSystem` bottom-bar toggle after `bottom_panel` exists. |
| `editor/editor_data.{cpp,h}` | STRUCTURAL | MEDIUM | `EditorData::EditedScene` gained an `EditorDocumentContext *document` member (memnew'd in the edited-scene path, ~line 630); added `get_document(idx)` / `get_active_document()`. Localized to the edited-scenes machinery. Note: this ownership model is exactly what the **Step ④ document split** revisits (EditorDocument/SceneDocument/EditorDocumentView) — these hunks will move, so don't polish them for rebase. |
| `editor/scene/canvas_item_editor_plugin.{cpp,h}` | STRUCTURAL | **HIGH** | The 2D services/view split (Step ⑤b.4). `CanvasItemEditor` is the 2D services singleton + a new instanceable `CanvasItemEditorView` (both in this file), mirroring the Node3DEditor split. Landed: display stack + pan/zoom (⑤b.4a), overlay (⑤b.4b), input/drag (⑤b.4c), document-binding (⑤b.4d — panes mint a view via `create_view_bound_to()`, each with its OWN `view_viewport` bound to the document's World2D; `CanvasView2D` deleted), and ⑤c (the MAIN view also renders through its own world-bound `view_viewport` via `activate_document()`, rebound per scene switch — the `_display_scene_root` reparent shim is gone). The SHIM surface (scene_root reparenting + `get_scene_view_container` forwarder) is fully retired; `scene_view_container` remains as the per-view host for `view_viewport`. High churn on both sides of a rebase; re-apply the *view ownership* intent. NEXT: this unblocks replacing the stock 2D\|3D\|Script main-screen switcher with the pane workspace. |
| `editor/editor_main_screen.{cpp,h}` | STRUCTURAL | LOW | Hosts the workspace: `EditorMainScreen` owns an `EditorWorkspace`; since G2 S5.5 the root pane hosts a `TabbedDocumentHost` whose tab 0 (the `ScreenHostDocument`) carries `main_screen_vbox`, parked under a hidden holder whenever its view dies so `get_control()` returns the live vbox at every instant (D11). `focus_editor()` reveals the screen-host tab before `_select_index`. UX delta: one tab bar above the main screen. M2.1 added a `// G2 D3` comment on the single make_visible driver in `_select_index` (comment-only). |
| `editor/script/script_editor_plugin.{cpp,h}` | STRUCTURAL | **HIGH** | The script services/view split (Milestone S, D7). `ScriptEditor` stays the scripting SERVICES singleton (45 external callers untouched); the per-script VIEW is workspace-hosted. S1: `registered_views` registry decouples "open scripts" from `tab_container` children (~21 all-open loops repointed). S2: `create_editor_view()` factory (create + wire without parenting). S6a: `edit()` routes through `EditorMainScreen::reveal` (scripts open as workspace tabs in the current pane); `_get_current_editor()` follows the workspace via `current_view_id` (ObjectID, pushed by TabbedDocumentHost/EditorWorkspace) with a legacy `tab_container` fallback; `get_window_layout`/`set_window_layout` enumerate/restore via the registry. S7 completed the retirement: `script_list`, the members/help overviews, `list_split`, and the internal `TabContainer` are DELETED — the workspace tab bar is the script list; the close family is surface-based (`_close_surface` → `EditorMainScreen::close_document` → the host's tab-close pipeline, with side effects centralized in `notify_surface_closing`); the shared chrome (`menu_hb` strip + `find_replace_bar`) reparents into the focused script/help tab's DocumentView with traveling shortcut contexts (seam #8); Make Floating is hidden/disabled (`toggle_files_panel`/`is_files_panel_toggled` remain as no-op stubs for EditorHelp/CodeTextEditor callers, and the `script_editor/toggle_files_panel` shortcut stays registered for their ED_GET_SHORTCUT lookups). ~4k-line file — on rebase re-apply the services/view intent; expect large conflicts anywhere upstream touches the deleted panel/tab machinery. |
| `editor/plugins/editor_plugin.cpp` | STRUCTURAL | LOW | **Comment-only (M2.1).** A `// G2 D3` doc-comment on `EditorPlugin::make_visible` pins the visibility contract: main-screen `make_visible` toggles the plugin's own singleton screen surface only; per-pane document views own their render visibility and must never be driven by it. No behavior change; trivial to re-apply on rebase. |
| `editor/gui/editor_bottom_panel.{cpp,h}` | ADDITIVE | LOW | **G4.** One new method `add_bottom_bar_toggle(text, icon, shortcut)` — adds a standalone toggle Button to the unified bottom bar (front of `bottom_hbox`) that hosts NO panel content; the caller drives its own surface (the `WorkspaceFileDrawer`) off the button's `toggled`. Purely additive; trivial to re-apply. |
| `editor/scene/scene_tree_editor.cpp` | **UPSTREAMABLE** | LOW | 9-line fix: `_selection_changed()` early-returns if `!is_inside_tree()` (the global `EditorSelection::selection_changed` signal can fire while the Scene dock is mid-reparent during layout restore → `_update_selection` resolves cached ABSOLUTE paths on an out-of-tree node → error). This is a **latent stock bug** (our multi-scene parking just exposes it), not flip-specific. **PR to `godotengine/godot`** (commit `637dba8048`); once merged upstream this hunk leaves our diff. Until then it's the smallest, safest divergence. |

## Added files (no conflict surface — additive only)

- `editor/editor_document.{cpp,h}` — the open-document model, split by state kind into `EditorDocument` (slim base) / `SceneDocument` (owns the world) / `EditorDocumentView` (per-pane view state). Renamed from `editor_document_context.{cpp,h}` in Step④ (2/n).
- `editor/gui/editor_workspace.{cpp,h}` — `EditorWorkspace` + `WorkspacePane` (the dividable pane tree).
- `editor/gui/document_view.{cpp,h}` — `DocumentView`: the per-pane presentation of one open document (owns the `EditorDocumentView` binding + hosts a `Node3DEditorView` on the document's world). Depends on `node_3d_editor_plugin.h` (gui→scene/3d, confined to this TU) — the permanent form of the ④-spike's temporary include.
- `editor/gui/tabbed_document_host.{cpp,h}` — `TabbedDocumentHost`: a pane's tabbed content (`TabBar` + lazily-created `DocumentView`s; selecting a tab swaps the rendered document). The first non-debug workspace UI. G6 added the drag-to-split drop handling (`can_accept_tab_drop`/`accept_tab_drop` + `_host_from_drag_data`, decoding the native TabBar rearrange payload) and hosts a `PaneDropOverlay` kept topmost over `content_host`.
- `editor/gui/pane_drop_overlay.{cpp,h}` — `PaneDropOverlay` (G6): the drag-to-split "compass". A mouse-transparent Control over a pane's content area that arms only during a document-tab drag (NOTIFICATION_DRAG_BEGIN/END → mouse_filter STOP/IGNORE), draws the 5-zone compass + a translucent split preview, and delegates the drop to its owner `TabbedDocumentHost`. Reuses the native `TabBar` drag payload/preview (no custom drag source). Split itself is `EditorWorkspace::move_tab_into_pane` (S8 detach/split/adopt engine).
- `editor/gui/workspace_file_drawer.{cpp,h}` — `WorkspaceFileDrawer` (G4): the slide-up file-exploration overlay. A top-level `PanelContainer` child of `center_vb` that tracks the column's rect and paints a centered, max-width band anchored to the bottom edge, animated by a Tween. Hosts the project `FileSystemDock` (commit 2). Mounted + toggled from `editor_node.cpp`.
- `workspace-editor-planning/**` — all planning docs, `ARCHITECTURE.md`, and the `smoke/` regression harness. Non-engine; zero rebase risk.

---

## Maintenance

- When you add or materially change a stock-file divergence, add/adjust its row
  here in the same commit. Keep the in-code tags (`// G1:`, `// G2 Step①:`) —
  the ledger says *which files*, the tags say *which hunks*.
- Re-verify the table against `git diff --name-status master...HEAD` at each
  milestone boundary (i.e. right before each rebase).

## Current G2 step notes

- `editor/gui/editor_workspace.{cpp,h}`: G2 M1.1 adds pane focus promotion on pre-GUI mouse-down / GUI focus-in, a multi-pane focus ring, `focused_pane_changed`, and `last_tabbed_pane` tracking for later `reveal()` target resolution.
- `editor/scene/3d/node_3d_editor_plugin.{cpp,h}`: G2 M1.2 carries the bound `EditorDocument *` into pane `Node3DEditorView`s and promotes that document on 3D mouse-down, mirroring the 2D `_ensure_active` gate.
- `editor/editor_document.h` and `editor/editor_main_screen.{cpp,h}`: G2 M1.3 adds `DocumentViewKind`, `reveal()`, and `focus_editor()`, while keeping `select()` / `select_by_name()` as compatibility shims.
- Script-screen intent callers under `editor/`: G2 M1.4 reroutes the documented `EDITOR_SCRIPT` sites to `focus_editor("Script")`; platform and mono callers remain untouched.
- `editor/editor_document.h`, `editor/gui/document_view.{cpp,h}`, `editor/editor_main_screen.{cpp,h}`: G2 S5.5 adds `TYPE_SCREEN_HOST` + `ScreenHostDocument` (seam #5) and makes the root pane a tab host from startup, so document reveals open as tabs in the current pane (no split-on-first-open). `DocumentView` gained a `NOTIFICATION_PREDELETE` handler — the destructor runs AFTER Node's PREDELETE frees children, so surface-detach work (script-view release, parking `main_screen_vbox`) must happen there; this also fixed S4's latent freed-pointer touch in `~DocumentView`.
- `editor/gui/editor_workspace.{cpp,h}` + `editor/gui/tabbed_document_host.{cpp,h}`: G2 S8 adds the pane management UX (both files are additive — no new stock conflict surface). `WorkspacePane::collapse_split()` absorbs the surviving child when a sibling closes; `EditorWorkspace::close_pane()` drains the pane's tabs through the host close pipeline (so script state caching / close notifications fire) then collapses, re-resolving `focused_pane`/`last_tabbed_pane` by ObjectID; `queue_close_pane()` defers by ObjectID so it is safe from inside the pane's own signal emissions. The tab bar gained a right-click context menu (Split Right/Down, Close Tab, Close Pane); splits MOVE the clicked tab — `detach_tab()`/`adopt_tab()` re-home the live `DocumentView` with no close side effects. Closing the last tab of a non-root pane closes the pane (deferred). Tab removal now preserves the current selection when a background tab is closed/moved.
- `editor/gui/editor_workspace.{cpp,h}`: **G2 M6.1 — geometry serialization** (additive; no new stock conflict surface). `WorkspacePane` gains a stable `pane_id` (assigned by `make_pane` from `EditorWorkspace::next_pane_id`) plus recursive `to_dict()`/`from_dict()` (members, so they reach the private split state). `EditorWorkspace::save_geometry()`/`load_geometry()` round-trip the whole split tree as a schema-v1 dict `{ v:1, next, root:<node> }` where a node is `{t:leaf,id}` or `{t:split,vert,off,a,b}`; `load_geometry` rebuilds leaves as empty `TabbedDocumentHost`s (the session store fills tabs in M6.2/M6.3), leaves `focused_pane` null for the restore driver to set, and refuses (leaving the live tree intact) on any version/structural anomaly. Split panes get `pane_id` 0 (only leaves persist ids). `find_pane_by_id()` added for the session store.
- `editor/editor_main_screen.{cpp,h}` + `editor/editor_node.{cpp,h}`: **G2 M6.2 — workspace-session persistence** (the pane layout survives restart). `EditorMainScreen::save_layout_to_config` additionally writes a `workspace` blob = `{ geometry: save_geometry(), tabs: {<pane_id>: {docs:[paths], cur:path}} }` (`_collect_pane_tabs` walks leaves; scene/script/help paths, `screens://Editor` for the screen-host, unsaved scenes skipped). **Restore is two-phase, and the split is load-bearing:** phase 1 `begin_workspace_restore()` runs from `EditorNode::_load_editor_layout` BEFORE `_load_open_scenes_from_config` — it rebuilds the geometry and re-homes the screen-host while **0 scene worlds are live**, because re-entering the legacy 2D/3D editors (carried in `main_screen_vbox`) with multiple worlds exhausts the per-world gizmo-layer budget and crashes; phase 2 `load_layout_from_config` runs after scene restore and fills each leaf's scene/script/help tabs (`_populate_pane_tabs`, screen-host skipped) then drives focus once. Both walks iterate `EditorWorkspace::get_tabbed_leaves()` (the split/leaf recursion stays in the workspace). `EditorData::get_or_create_document_for_path()` maps a saved path → live doc (open scene by path / `get_or_create_*` for script+help). Restore-in-flight is a single source of truth — `EditorMainScreen::is_workspace_restore_pending()` (non-empty `_pending_tabs`), which `_ensure_active_scene_tab` pulls to stand the M7.1 scene auto-reveal down. Legacy configs (no `workspace` key) fall through to the stock strip `select()`. Rebase note: additive + STRUCTURAL (the two-phase ordering is ours-by-design). Regression net: `smoke/restore_workspace.cfg` (nested 3-pane + screen-host) + the `save_restore_roundtrip` smoke case (exercises the SAVE side the fixture bypasses).
- **G2 M6.2 follow-ups (post-test fixes):** (1) `editor/editor_document.{h,cpp}` — `EditorDocument::get_path()` is now `virtual`; `SceneDocument::get_path()` derives from `root->get_scene_file_path()` (the base `path` field is never populated for scenes). Without it every scene serialized with an empty path and was dropped from the saved workspace tabs — the "panes restored empty" bug. (2) `editor/docks/editor_dock_manager.cpp` — close the D8 retire-enforcement gap: `load_docks_from_config` skips retired docks (a pre-D8 saved `docks` slot no longer resurrects Signals/Groups) and `set_dock_enabled` refuses to re-enable a retired dock (the feature-profile pass was re-enabling them). (3) `editor/gui/document_view.cpp` — the accordion sections override FoldableContainer's `focus` stylebox with an empty box; the theme's default focus stylebox is a square accent outline that poked out behind the cards' rounded corners (the "blue behind the rounded tops"). Known separate pre-existing gap (NOT M6): a lone open scene isn't auto-revealed into a pane (M7.1 only reveals on a scene *change*).
- `editor/editor_document.h` + `editor/editor_data.cpp`: G2 D1 brought the per-document `SceneDocument::selection` (`EditorSelection`, memnew'd in the ctor) alive and added `EditorDocument::is_scene()` (gates the `static_cast<SceneDocument *>` cast-free). D1's write-behind save/restore *mirror* was a stepping stone **superseded by D2** — see below; `save/restore_edited_scene_state` are back to the stock `es.selection` List snapshot (D2 needs the List, not the doc object, to avoid self-erase — the proxy forwards `clear()` to the very selection a doc-object read would replay from).
- `editor/editor_data.{h,cpp}` + `editor/editor_node.cpp`: **G2 D2 — Model B**. New `EditorActiveSelectionProxy : EditorSelection` (in `editor_data.{h,cpp}`): the single stable selection object every global consumer + the 5 cached dock/editor pointers hold, forwarding the consumer method set (add/remove/clear/update/is_selected/the top+full node lists/`get_selected_nodes`/`get_selection` map/`get_node_meta`) at the **active document's own** `EditorSelection`, and relaying that target's `selection_changed` as its own so object + signal identity never change. The forwarded set is now `virtual` on `EditorSelection`; the `get_node_editor_data<T>` template dispatches through a new `virtual get_node_meta()` (templates can't be virtual). Metadata providers registered via `add_editor_plugin` are remembered and seeded onto each target on first activation (`has_editor_plugins()` guards re-seed). NOT a GDCLASS — it deliberately reports as `EditorSelection` so consumers and the inherited signal are unaware. `EditorNode` allocates the proxy (`editor_node.cpp` ctor) and retargets it in `_activate_scene_views()` (silent — the switch path's existing save/restore refresh drives consumers). **Transparent by design:** save/restore/clear and the reload/layout paths are untouched, so behavior is identical today (one active document ⇒ one target); the per-document authority this establishes is what D7's per-pane docks bind to. Rebase note: the `virtual` additions + proxy are ours-by-design (STRUCTURAL); re-apply the "retarget the active document's selection" intent.
- `editor/editor_document.h` + `editor/editor_node.{h,cpp}`: **G2 M7.1 — scenes open as workspace tabs**. `SceneDocument::opens_as_workspace_tab()` flips to true, so `reveal(scene)` routes through the pane/`DocumentView` path (minting a per-pane `create_view_bound_to` viewport) that existed but was dormant. `_set_current_scene_nocheck` schedules a deferred, no-focus-steal `_ensure_active_scene_tab()` (helper resolves the active doc + `reveal(doc, DEFAULT, grab_focus=false)`; deferred + no-arg to avoid reentrancy and enum/Variant marshalling). **Coexistence fix (the non-obvious part):** `_activate_scene_views()` now *unbinds* the legacy singleton 2D/3D main views for pane-hosted docs (`activate_document(nullptr)` / `set_active_world(Ref())` — the same no-world path the empty-project case already used) — otherwise the scene renders in BOTH its pane view and the legacy main view, exhausting the per-world gizmo cull-mask layers (smoke's restore-3-scenes caught this as ">5 simultaneous 3D views"). Interim state after M7.1: scene panes have no 2D/3D toolbar (travels in M7.2a) and boot lands on the now-scene-less legacy main-screen tab (default-surface flip is M7.2b). Rebase note: STRUCTURAL; the flip + stand-down are ours-by-design.
- **G2 D5–G3 catch-up (per-pane editing + composite docks), commits `c26d54e8e9`..`06fa627077`:**
  - `editor/docks/scene_tree_dock.{h,cpp}`, `editor/docks/inspector_dock.{h,cpp}`, `editor/docks/signals_dock.{h,cpp}`, `editor/docks/groups_dock.{h,cpp}`: **bound-instance mode** (`p_is_global` ctor flag, default true). Only the one global dock claims its `singleton` and registers its `docks/open_*` `ED_SHORTCUT_AND_COMMAND`; dtors clear the singleton only if they own it. Lets N per-pane instances coexist (D5, D7b, G3). InspectorDock also gained `edit_bound()`.
  - `editor/scene/scene_tree_editor.{h,cpp}`: **per-instance scene-root override** (`scene_root_override` ObjectID + `set_scene_root_override`). `get_scene_node()` prefers it over the global `get_tree()->get_edited_scene_root()`, so a bound per-pane tree keeps its own scene across tab switches (D7a-fix). The global tree leaves it empty (unchanged).
  - `editor/gui/document_view.{h,cpp}`: **the composite scene pane** — `[toolbar host | viewport] | dock-column` where the dock column is a `FoldableContainer` accordion (Scene Tree + Inspector expanded, Signals + Groups folded), each section a bound dock driven from the document's own selection (`_bound_selection_changed`). Additive (new file).
  - `editor/scene/3d/node_3d_editor_plugin.{h,cpp}`: (a) `set_view` resolves its container's own viewports (not the singleton's) so pane views control their 1/2/3/4 layout; pane views default to 1 viewport. (b) `editor_views` registry — `update_transform_gizmo` fans out to all live views so the focused pane renders the transform gizmo; ENTER_TREE re-applies the bound world to migrate freshly-created gizmo instances into the document scenario. (c) `main_flow` toolbar exposed via `get_shared_toolbar()`/`park_shared_toolbar()`.
  - `editor/scene/canvas_item_editor_plugin.{h,cpp}`: `main_flow` toolbar exposed the same way (M7.2a).
  - `editor/editor_node.{h,cpp}`: `_activate_scene_views` unbinds the legacy 2D/3D main views for pane-hosted docs (M7.1 double-render fix); `update_scene_pane_toolbar()` mounts the matching 2D/3D toolbar into the focused scene pane / parks it (M7.2a), pushed from the same tab-select/pane-focus points as `ScriptEditor::set_current_surface`.
  - **Deferred / known-open:** legacy strip retirement + boot-to-scene default surface (M7.2b) not done — scenes-as-tabs coexists with the legacy strip; a few 2D/3D plugin sites still ask the global `get_edited_scene()` (residual `is_editable_instance` cross-scene noise, non-fatal); dock styling (bevel/accordion) is a later theming pass.
- `editor/plugins/editor_plugin.cpp` + the 2D/3D plugin `make_visible` bodies: G2 M2.1 is an **audit + tags** step (no behavior change). Verified that main-screen `make_visible` cannot blank/freeze pane views: 2D show/hide/process reach only the singleton `main_view`, and the `viewport_set_disable_2d`/`environment` calls hit the ACTIVE doc's parked `scene_root` SubViewport (which no view displays — every view renders its World2D through its own `view_viewport`); 3D show/hide reaches only the singleton `main_view`, and `set_process`/`set_physics_process` gate only the singleton's own (near-empty) callbacks while each `Node3DEditorViewport` self-drives from `NOTIFICATION_VISIBILITY_CHANGED`. Contract documented on `EditorPlugin::make_visible` and tagged `// G2 D3` at the two plugin bodies + the `_select_index` driver. Note: the 2D scene_root viewport toggles are now effectively inert (candidate for a later `/simplify` cleanup) — left in place to keep the audit behavior-identical.
